# 카드5 — 이름을 가리고 비교 / 제출 준비

## 제출물

- **결과물 URL**: https://gamedh823-cmd.github.io/T05/
- **소스 저장소 URL**: https://github.com/gamedh823-cmd/T05/blob/99cb04a6beffb34e7d1a98b24fce47e815816dc3/index.html

## 재현·통과 확인 4가지

**어디로 가나요**
위 결과물 URL로 접속합니다. 로그인·회원가입·설치·CAPTCHA 없이 새 시크릿 창에서 바로 열립니다.

**무엇을 하나요 (3단계 이내)**
1. 시·도/시·군·구/읍·면·동을 선택합니다.
2. 지역 제목 아래 "⭐ 이 지역 즐겨찾기 추가"를 누릅니다.
3. 생성된 즐겨찾기 칩을 클릭합니다.

**무엇이 보이면 통과**
칩이 목록에 추가되고, 칩을 클릭하면 select 값과 날씨가 그 지역으로 자동 전환되며, 새로고침해도 즐겨찾기 목록이 그대로 남아 있으면 통과입니다.

**안 될 때**
즐겨찾기가 8개를 넘으면 추가되지 않고 "즐겨찾기는 최대 8개까지 저장할 수 있습니다" 문구가 뜨고, 모두 삭제하면 "즐겨찾기한 지역이 없습니다" 문구가 뜨며, 저장된 값이 손상돼도 오류 없이 빈 목록으로 안전하게 초기화됩니다.

## AI와 나의 판단 3줄

**AI에게 맡긴 일**
즐겨찾기 배열 저장 구조·칩 UI 렌더링·삭제/전환 로직의 초안 구현(AI A), 남은 검사 3개(FAV-08~10)에 대한 코드 제안(AI B).

**내가 판단한 일**
"이 지역 즐겨찾기 추가" 버튼을 좌표 옆(맥락이 애매한 위치)에서 지역 제목 바로 아래로 옮겼습니다. `.favorite-chip button` 선택자가 이름 버튼까지 20×20으로 찌그러뜨리는 CSS 버그를 발견해 삭제 버튼 전용 클래스로 분리해 고쳤습니다. AI B(Gemini)가 스스로 "완료했다"며 보고한 작업 시간·호출 수·커밋 해시가 저장소를 실제로 확인하지 않은 지어낸 값이라는 걸 확인하고, 코드를 직접 반영·브라우저 재검증·실제 커밋을 거쳐 진짜 값으로 바꿨습니다.

**AI 말을 안 들은 일**
Gemini는 즐겨찾기 개수 초과 시 브라우저 `alert()`로 막자고 제안했지만, 기존 화면의 안내 방식(페이지 안 문구)과 톤이 맞지 않아 인라인 문구로 바꿔 반영했습니다.

## 완주 체크리스트

- [x] 검사 10개와 공통 사용 상한을 먼저 고정했습니다. (`카드1_고정스펙.md`)
- [x] A 시작→A 인계→B 시작→B 완료 순서가 커밋 로그와 맞습니다.
- [x] 인수인계 7항목을 새 환경(Gemini 새 대화)에서 재현했습니다 — **단, Gemini는 파일 수정·실행 도구가 없는 웹 채팅이라 코드를 "제안"까지만 했고, 실제 반영·실행·재검증은 AI A(Claude Code)가 대신했습니다.** 아래 참고.
- [ ] → [x] AI B가 저장소·인수인계만으로 같은 검사를 실행했습니다 — **Gemini 자신은 실행할 수 없었으므로, AI A가 Gemini의 제안을 그대로 코드에 반영한 뒤 검사 10개를 직접 실행해 확인했습니다.** (아래 "환경 차이" 참고)
- [x] 이름을 가린 측정표와 도구 선택 기준을 확인했습니다. (아래 표)

### 환경 차이에 대한 메모
이번 과제에서 AI B로 쓴 서비스(Gemini 웹 채팅)는 저장소 파일을 직접 열거나 수정·커밋할 도구가 없어서, "인수인계 문서만 보고 새 환경에서 이어받아 실행"하는 과정의 뒷부분(코드 반영, 브라우저 검사, 커밋)을 AI A 역할을 했던 도구(Claude Code, 파일·터미널 접근 가능)가 대신 수행했습니다. Gemini는 인수인계 문서와 코드 파일만 보고 무엇이 왜 남았는지 정확히 파악해 올바른 코드를 제안했다는 점에서 "문서만으로 이어받기"는 성립했지만, "실행까지 스스로 완결"하지는 못했습니다. 이는 두 AI 환경의 실제 차이이자 이 과제가 드러내려던 지점이라고 판단해 감추지 않고 그대로 적었습니다.

## 이름을 가린 비교표

| 구분 | AI 환경 A | AI 환경 B |
|---|---|---|
| 실제 작업 방식 | 파일·터미널 도구로 직접 구현·실행·커밋 | 채팅으로 코드 제안만 (반영·실행은 A가 대신) |
| 대화 왕복 수 | 2회 | 8회 (그중 코드 제안에 해당하는 왕복 2회) |
| 시간 상한 | 없음 (합의로 시간 대신 요청 수만 상한으로 둠) | 없음 |
| 검사 통과 수(그 단계에서 담당한 범위 기준) | FAV-01~07 통과 (7/10, 나머지는 인계) | FAV-08~10 제안이 반영 후 통과 (최종 10/10) |
| 오류(FAIL) 발생 | 구현 중 CSS 버그 1건 자체 발견·수정 (제출본엔 없음) | 없음 (제안 코드가 반영 후 한 번에 통과) |

**다음 작업에서 도구를 고르는 기준**: 파일을 직접 만들고 실행·검증까지 해야 하는 작업은 도구 접근 권한이 있는 AI에게 맡기고, 코드 설계 검토나 아이디어 제안처럼 텍스트로 끝나는 작업은 웹 채팅형 AI에게 맡긴다.
