# 카드3 — 인수인계 문서 (AI A가 작업을 멈출 때 채운다)

> 이 문서 + 저장소(코드)만 AI B에게 넘긴다. 대화 전문은 넘기지 않는다.

## 1. 목표
과제4 "전국 상세 날씨 정보판" 코드베이스에 "즐겨찾는 지역 여러 개" 기능 추가. 상세 요구사항·고정 검사 10개는 `카드1_고정스펙.md` 참고.

## 2. 현재 상태
지역 제목(`selected-region-title`) 옆 "⭐ 이 지역 즐겨찾기 추가" 버튼(`add-favorite-btn`)과 그 아래 즐겨찾기 칩 목록(`favorites-list`)을 구현했다. 현재 지역을 즐겨찾기에 추가·중복 방지·여러 개 순서대로 추가·새로고침 후 유지·칩 클릭으로 지역 전환 및 자동 날씨 조회·칩 삭제까지 동작한다. FAV-01~FAV-07은 통과. **최대 개수 제한(8개), 빈 상태 문구, 저장값이 깨졌을 때의 안전 초기화(FAV-08~FAV-10)는 아직 구현하지 않았다.**

## 3. 실행 명령
설치·빌드 필요 없음. `HW5/index.html`을 브라우저로 직접 열거나, 정적 서버로 `HW5` 폴더를 띄우면 된다(예: `python -m http.server 8791 --directory HW5`).

## 4. 통과·검사

| ID | 결과(통과/실패/미착수) |
|---|---|
| FAV-01 | 통과 |
| FAV-02 | 통과 |
| FAV-03 | 통과 (서로 다른 지역 2곳으로 확인함 — 4곳까지 늘려 재확인 권장) |
| FAV-04 | 통과 |
| FAV-05 | 통과 |
| FAV-06 | 통과 |
| FAV-07 | 통과 |
| FAV-08 | 미착수 |
| FAV-09 | 미착수 |
| FAV-10 | 미착수 |

## 5. 남은 문제
- **최대 개수 제한 없음**: `addCurrentToFavorites()`(`app.js`)에 `MAX_FAVORITES`(8) 상수는 이미 선언돼 있지만 아직 사용하지 않는다. 8개 초과 시 추가를 막고 안내 문구를 보여줘야 한다(FAV-08).
- **빈 상태 문구 없음**: 즐겨찾기가 0개일 때 `favorites-list`가 그냥 빈 채로 남는다. "즐겨찾기한 지역이 없습니다" 같은 안내가 필요하다(FAV-09).
- **손상된 localStorage 값 방어 미검증**: `loadFavorites()`에 try/catch로 깨진 JSON을 빈 배열로 되돌리는 코드는 있지만, 실제로 브라우저에서 값을 깨뜨려 새로고침해보는 검증은 하지 않았다(FAV-10). 코드상으로는 될 것으로 예상하지만 직접 확인 필요.

## 6. 다음 행동
1. `addCurrentToFavorites()`에 `favorites.length >= MAX_FAVORITES`일 때 추가를 막고 안내 문구(예: 버튼 아래 임시 텍스트, 몇 초 후 사라짐)를 보여주는 로직 추가 → FAV-08 확인.
2. `renderFavorites()` 시작 부분에서 `favorites.length === 0`이면 "즐겨찾기한 지역이 없습니다" 같은 문구를 `favorites-list` 안에 렌더링 → FAV-09 확인.
3. 브라우저 개발자도구 콘솔에서 `localStorage.setItem('koreaDetailedWeatherFavoritesV5', '{깨진 json')` 실행 후 새로고침해 페이지가 안 죽고 빈 목록으로 뜨는지 확인 → FAV-10.
4. FAV-01~FAV-10 전체를 처음부터 다시 한 번 순서대로 눌러보며 재확인.
5. 완료되면 카드5(비교) 진행을 위해 종료 시각·저장소 버전·실제 사용량을 이 문서 맨 아래에 기록.

## 7. 건드리지 말 것
- 기존 날씨 조회 로직(`fetchWeather`, `buildWeatherUrl` 등)과 행정구역 select 로직(`populateSigungu`, `populateDong`, `updateSelectedRegion`)은 정상 동작하니 손대지 않는다.
- 기존 단일 지역 선택 저장(`SELECT_KEY` / `saveSelection` / `restoreSelection`)도 그대로 유지한다 — 즐겨찾기는 별도 키(`FAVORITES_KEY`)를 쓴다.
- `.favorite-chip button`처럼 범위가 넓은 CSS 선택자를 쓰지 않는다 — 이름 버튼과 삭제 버튼을 동시에 건드려 레이아웃이 깨진 적이 있다(지금은 `.favorite-chip-remove`로 분리해둠).

---
- AI A(Claude) 종료 시각: 2026-09-03 (오전)
- AI A 종료 시점 저장소 버전(커밋 해시): `db942951f2a23d3894437e3cbc7a6c93857c4985`
- AI A 실제 사용량: 대화 왕복 2회 / 상한 30회 이내

## AI B 기록 (Gemini)

- AI B 종료 시각: 2026-09-03 오전 10:36
- AI B 최종 커밋 해시: `99cb04a6beffb34e7d1a98b24fce47e815816dc3`
- AI B 실제 사용량: 대화 왕복 8회 / 상한 30회 이내

FAV-08 관련해서 Gemini는 초과 시 `alert()`로 막는 코드를 제안했는데, 기존 화면의 안내 방식(인라인 문구)과 맞지 않아 페이지 안 문구로 바꿔 반영했다.
