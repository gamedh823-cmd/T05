# 카드1 — 같은 문제, 같은 검사 (고정 스펙)

이 문서에 적힌 내용은 **AI A 작업을 시작하기 전에 고정**하며, 이후 카드2~5 전체 과정에서 바꾸지 않는다.

## 기반 프로젝트
- 과제4 "전국 상세 날씨 정보판 v5" (`HW4/`)를 그대로 복사한 `HW5/`에서 이어서 작업한다.
- 시작 소스 주소(고정 커밋):
  → `https://github.com/gamedh823-cmd/T05/blob/5ca6abd90a4d228d98458b5fc19de81fe45f4034/index.html`

## 추가할 개선 기능 한 가지
**즐겨찾는 지역 여러 개 — 다중 즐겨찾기 저장 및 클릭 전환**

- 상단 지역 선택 영역(`region-bar`)에 "⭐ 즐겨찾기 추가" 버튼과 즐겨찾기 칩 목록을 추가한다.
- 사용자가 시·도/시·군·구/읍·면·동을 선택한 상태에서 "즐겨찾기 추가"를 누르면 현재 지역이 즐겨찾기 목록에 칩으로 추가된다.
- 즐겨찾기 칩을 클릭하면 그 지역으로 select 값이 바뀌고, 기존 "선택 지역 날씨 보기"(`weather-btn`)를 누른 것과 같은 효과로 날씨 조회가 자동 실행된다.
- 각 칩에는 삭제(×) 버튼이 있어 개별 삭제할 수 있다.
- 즐겨찾기는 최대 8개까지 저장되며, 새 localStorage 키(예: `koreaDetailedWeatherFavoritesV5`)에 저장되어 새로고침해도 유지된다.
- 기존 단일 지역 선택 저장(`SELECT_KEY`, `koreaDetailedWeatherSelectionV5`)이나 날씨 조회 로직은 건드리지 않는다.

## 공통 사용 상한 (AI A·AI B 각각 개별 적용)
- **시간 상한**: 없음 (짬짬이 작업하므로 시계가 아니라 실제 대화 왕복 횟수로 관리)
- **요청·호출 수 상한**: **30회** — AI A가 30회, AI B가 별도로 30회. (합산이 아니라 각자 30회씩)

## AI A·AI B에 공통으로 쓸 최초 요청 (그대로 복사해서 사용)
ChatGPT(AI A)에게: 새 대화를 열고, `HW5/index.html`, `HW5/app.js`, `HW5/style.css` 세 파일을 첨부한 뒤 아래 메시지를 그대로 보낸다.

> 첨부한 index.html / app.js / style.css는 "전국 상세 날씨 정보판" 웹앱이야 (소스: https://github.com/gamedh823-cmd/T05/blob/5ca6abd90a4d228d98458b5fc19de81fe45f4034/index.html). 여기에 "즐겨찾는 지역 여러 개" 기능을 추가해줘. 요구사항은 아래와 같고, 고정 검사 10개(FAV-01~FAV-10)를 모두 통과해야 해. 통과 여부는 내가 직접 브라우저에서 눌러보며 확인할 거야. 기존 지역 선택/날씨 조회 기능은 그대로 유지해줘.
>
> [여기에 이 문서의 "추가할 개선 기능 한 가지"와 "고정 검사 10개" 표를 함께 붙여넣는다]

같은 최초 요청(위 문구 + 기능 설명 + 검사 10개 표)을 나중에 AI B에게도 그대로 다시 보낸다 (단, AI B에게는 이 시점의 최신 코드 + 카드3 인계 문서를 함께 준다).

## 고정 검사 10개

| ID | 입력(조작) | 기대값(관찰 가능한 결과) |
|---|---|---|
| FAV-01 | 지역 하나(예: 서울특별시 강남구 역삼동)를 선택하고 "즐겨찾기 추가" 클릭 | 즐겨찾기 목록에 "서울특별시 강남구 역삼동" 칩이 정확히 1개 나타난다 |
| FAV-02 | 같은 지역에서 "즐겨찾기 추가"를 한 번 더 클릭 | 중복 추가되지 않고 즐겨찾기 목록은 여전히 칩 1개다 |
| FAV-03 | 서로 다른 지역 4곳을 순서대로 추가 | 즐겨찾기 목록에 정확히 4개 칩이 추가한 순서대로 나타난다 |
| FAV-04 | 위 상태에서 새로고침(F5) | 즐겨찾기 칩 4개가 새로고침 후에도 그대로 남아 있다 |
| FAV-05 | 즐겨찾기 목록에서 현재 선택 지역이 아닌 칩을 클릭 | 시·도/시·군·구/읍·면·동 select와 "선택 지역" 제목이 그 칩의 지역명으로 바뀐다 |
| FAV-06 | FAV-05 클릭 직후 | "선택 지역 날씨 보기"를 직접 누른 것과 같이 로딩 상태 표시 후 날씨 결과가 자동으로 표시된다 |
| FAV-07 | 즐겨찾기 칩 하나의 삭제(×) 버튼 클릭 | 그 칩만 목록에서 사라지고 칩 개수가 1개 줄어든다 |
| FAV-08 | 즐겨찾기를 8개까지 채운 뒤 9번째 지역에서 "즐겨찾기 추가" 클릭 | 9번째는 추가되지 않고 "최대 8개까지 저장할 수 있습니다" 같은 안내 문구가 뜨며 목록은 8개로 유지된다 |
| FAV-09 | 즐겨찾기를 모두 삭제 | 목록 영역에 "즐겨찾기한 지역이 없습니다" 같은 빈 상태 문구가 보이고 칩은 0개다 |
| FAV-10 | 브라우저 개발자도구에서 `koreaDetailedWeatherFavoritesV5` 값을 깨진 문자열로 바꾼 뒤 새로고침 | 콘솔 에러로 페이지 전체가 멈추지 않고, 즐겨찾기 목록이 빈 상태(0개)로 안전하게 초기화된다 |

## AI A 시작 기록 (작업 시작 시 채움)
- 시작 시각: ______
- 시작 소스 버전(커밋 해시): ______
- 사용한 AI/서비스: ChatGPT (____ 버전)
